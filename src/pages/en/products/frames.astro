---
import Layout from '../../../layouts/Layout.astro'
import ProductTitle from '../../../components/ProductTitle.astro'
import CardProduct from '../../../components/CardProduct.astro'

import { getI18N } from '../../../i18n/'
const { currentLocale = 'es' } = Astro
const i18n = getI18N({ currentLocale })
---

<Layout title={i18n.FRAMES.TITLE} description="">
	<ProductTitle productType="FRAMES" />
	<CardProduct productType="FRAMES" />
</Layout>
